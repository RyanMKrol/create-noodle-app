{
  "name": "<%= projectName %>",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "npm run-script build && ts-node build/index.js"
  },
  "license": "MIT",
  "devDependencies": {
    "ts-node": "^7.0.1",
    "typescript": "^3.0.3"
  }
}
